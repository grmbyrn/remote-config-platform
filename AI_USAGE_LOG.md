# AI Usage Log

**Tool used:** Claude (Opus), primarily through Claude Code in VS Code, with some
planning conversations in the Claude desktop app.

**What I used it for:** understanding and pulling apart the brief. Exploring
alternatives before committing to an approach (concurrency strategy, repo
structure, response shaping), debugging, reviewing my own code for bugs I'd
missed and checking deployed state against local config before redeploying.

**How I evaluated the output:** the dated entries below are the record. Where a
suggestion was wrong or didn't fit the design, I've noted what I changed and why.
Where it was right, I've noted how I verified it rather than just accepting it.
Corrections in both directions are included.

## 17/07/2026

- Reviewed the case study brief with Claude to separate explicit requirements from ambiguous sections left open to interpretation (e.g. country audience UI, AI-targeting logic). Asked Claude to flag which parts were its own suggestions rather than stated requirements.
- Discussed four approaches to preventing concurrent-edit conflicts (optimistic locking with a version field, Firestore's native updateTime precondition, pessimistic locking, last-write-wins). Chose the explicit version-field approach since it's easier to explain and defend, and fits cleanly with the country-override feature.
- Weighed monorepo vs. two separate GitLab repos against the "separation of services" requirement. Went with a monorepo with clean folder separation to avoid doubling reviewer-access admin overhead.
- Built the Firestore schema iteratively, then cross-checked it against the actual panel screenshots provided. Caught two missing fields (description, createdAt) that were visible in the screenshots but absent from the draft schema.
- Debugged a sign-in form where the error message wasn't rendering. Root cause was a catch (error) block shadowing an outer error ref of the same name. Verified the fix by retesting both success and failure paths manually.
- Flagged that Claude's initial backend scaffold wired handler logic directly into the route files, with no controller layer. Restructured it into a routes → controllers → services split: routes only map paths to handlers, controllers own the HTTP request/response concerns, and services own data access (Firestore). This keeps business logic out of the route files, makes the data layer unit-testable in isolation and swappable without touching HTTP code, and turns the brief's "separation of services" from aspirational into something actually enforced by the structure.
- Used Claude to sketch rough wireframes for two screens not in the provided screenshots (country override editor, AI suggestion review), then had them re-rendered to match the real panel's color palette and typography.

## 18/07/2026

- I had Claude cross-check the actual code against the README's status table before starting, and it turned up two things listed as done that weren't there yet — the requireFirebaseAuth middleware and the frontend route guard. Corrected the plan to build them.
- Claude's first cut of the create endpoint used .set(); I switched it to .create() so a duplicate key returns a 409 instead of silently overwriting existing config and resetting its version.
- Pushed Claude to settle the version-conflict 409 body (current value/version/updatedBy) before writing the transaction, so the service, controller and frontend shared one contract — and kept it distinct from the duplicate-key 409.
- Caught two input traps in Claude's suggestions: guarded with value === undefined instead of !value (so false/0/"" aren't rejected), and coerced expectedVersion to a number so a stringified value wouldn't cause a phantom conflict.
- Asked Claude how to build the route guard. It flagged that auth.currentUser is null on a hard refresh until Firebase restores the session and gave an onAuthStateChanged approach that waits for the initial auth state. Verified it by testing the refresh case, which is exactly where the naive version would have bounced a logged-in user.
- Claude's create-row plan assumed a type-aware value input. I checked it against the panel screenshot, saw there's no type selector there, and had it default type to "string" instead — deferring the typed input rather than building past the design.
- Claude flagged that my update controller had the NOT_FOUND and VERSION_CONFLICT statuses swapped (404 vs 409). It bit exactly as predicted when the two-tab conflict returned 404 and the clean message didn't fire, so I fixed the mapping to return a recoverable 409.
- Claude's diagnosis of a 500 on create was a missing description being written as undefined. That wasn't it. The backend log showed a ReferenceError, and the real cause was my own rename: the controller was calling createParameterHandler instead of the createParameter service, so the handler called itself. Checked the other three call sites afterwards.
- Tested explicitly that a stale PUT against a deleted parameter returns 404, not 409, since the transaction checks existence before comparing versions. Used a deliberately wrong expectedVersion so the two paths couldn't be confused.
- Claude pointed out that the current block in a 409 response is read inside the transaction, so it can't prove the rejected write didn't land. Re-read the document in Firestore afterwards and confirmed version, value and updatedAt were all untouched.
- Decided not to version-check DELETE after asking for the tradeoff. The interleaving that matters — deleting while another manager edits — is already safe, because the update transaction checks existence first and returns a 404. Recorded it in the README as a deliberate omission rather than leaving it unexplained.
- Settled that PUT stays value-only, with type immutable by design: country overrides and AI suggestions are both validated against a parameter's type, so letting it change would strand data that no longer matches. Logged description being uneditable as a genuine gap rather than dressing it up as a decision.

## 19/07/2026

- Had Claude check the deployed Cloud Run env vars against backend/env.yaml before I redeployed. The live service had the deployed frontend origin set on it, but env.yaml still said localhost, so the next deploy would have silently reset CORS and broken sign-in on the live panel in a way that presents as an auth error, not a CORS one. Fixed the file rather than the service, since env.yaml is what deploys actually read.
- Claude's plan for /config said to strip the panel-only fields from each document. I built the response as a key/value whitelist instead. Same output today, but when countryOverrides lands tomorrow it can't leak by my forgetting to add it to a strip list.
- Decided /config should await the first Firestore snapshot before the server starts listening, so there's no window where it serves an empty object to a mobile client. The point was to make it a decision rather than an accident.
- After redeploying I was about to tick off the "listener works without a restart" check, but Claude pointed out the deploy had created a fresh revision, so the value I was looking at only proved the startup snapshot worked. Re-tested by editing a parameter after the deploy. That change can only have arrived through the listener.
- Claude flagged that requireFirebaseAuth checked startsWith('Bearer') without the trailing space while still slicing 7 characters, so a malformed header would pass the check and get mis-sliced. Fixed it, and used the corrected pattern in the new requireApiToken.
- Asked what the static API token actually is, since it's just a random string. It's compared, not verified — no identity, no expiry — unlike the Firebase ID token the panel sends, which is why only panel writes can carry an updatedBy. Noted that the plain string comparison leaks a small timing signal; not worth defending against here, but it belongs in the README's limitations rather than going unmentioned.

## 20/07/2026

- Tested the create path with an invalid value and got a generic 500 instead of a 400. The validation was correctly throwing a coded error from the one shared helper that create, update and override all call, but the controllers weren't mapping it, so I added the INVALID_VALUE branch to both the create and update handlers and re-ran the invalid case to confirm the 400.
- Claude recommended leaving the parameter list sorted alphabetically by key. I checked it against the panel screenshot, which sorts by Create Date with a direction toggle, and built that instead. Sorted client-side so the arrow flips without a refetch.
- Kept the type selector on the create row even though it isn't in the provided screenshots. The design predates the type requirement, and inferring the type from the value is ambiguous ("14" could be a string or a number), so an explicit selector styled to match was the honest tradeoff rather than guessing.
- Chased a bug where every boolean saved as false even with the box ticked. The checkbox was firing my handler on the input event, which runs before v-model syncs the value on change, so it always read the previous state; moving the handler to change fixed it, and /config then returned a bare true.

## 21/07/2026

- Reused the create row's value input for the override modal instead of building a second one. The snag was pre-filling it to edit an existing override: Claude's first suggestion was a reactive watch on the bound value, but that feeds the component its own emitted output and wipes a half-typed number like "2." back to "2". Seeded it once on mount and forced a remount with a key when opening an edit instead, so typing is never clobbered.
- Claude spotted a missing return in the override-remove handler. After setting the conflict state on a 409 it fell through to the generic not-ok throw, so a legitimate version conflict also flashed an error message. Added the return and re-ran the two-tab remove to confirm only the conflict dialog shows.
- Chased an override save that only landed on the second click. With two panel tabs open it was the version check firing a real 409 on the first save, but the conflict dialog had no z-index and was rendering behind the override modal, so I never saw it and the rejection had already refreshed my version in place, which is what let the second click through. Layered the dialogs; the conflict then read as the concurrency control working rather than a bug.
- The conflict dialog, reused from the inline row edit, showed a blank "Your value" on override conflicts because it read the inline-edit field, which is empty during an override. Carried the attempted value through the conflict object so it reads correctly for both paths.
- A country field in the modal looked like it wasn't taking input. The text was going in but rendering invisible. The input inherited white text from the dialog but kept the browser's default light background because I'd left off the shared .field class. Added the class and it matched the other inputs.
- Added an explicit per-row Override button even though the feature has no provided mockup, reasoning that reviewers have to find and test the TR scenario and a discoverable control beats matching a design that doesn't cover it. Styled it as a secondary/outline action with an override count so it doesn't compete with the Edit and ADD buttons.
- Before ticking off the deployed country checks, ran /config against the live API and every country returned the default. Compared it to my local backend on the same Firestore, which resolved the TR override correctly, so the live service was a stale build from before the resolver existed, not a data or code problem. Redeployed and re-ran the checks to confirm TR resolved on the live URL.
- Chased a 502 from the new AI suggestions endpoint that looked like a bug in my code. Calling the Anthropic API directly showed the real cause (the account had no credit) so the request was failing upstream and my error mapping was correctly surfacing it as a 502 rather than crashing. A billing problem wearing a bug's clothes; topped up and it worked.
- Even with credit the endpoint still failed, and the direct call explained it: the model wrapped its JSON in a ```json code fence despite the prompt saying not to, so JSON.parse threw. Rather than trust the model to obey the instruction, I now parse the substring from the first { to the last }, so a fence or stray prose can't break it. Chose that over an assistant-prefill because it tolerates more kinds of junk, not just fences.
- Proved a wrong-typed suggestion is dropped by exercising the shared isValidValue helper directly instead of hoping the model would emit garbage on cue. "true" for a boolean and "1.4" for a number both return false, so a mismatched value never reaches the stored map. Confirmed the valid ones had really persisted by re-reading them through the list endpoint, since the save returns its own in-memory object rather than a fresh read.
- Decided that re-running generation overwrites an existing pending suggestion rather than skipping it: a pending suggestion is unapproved with nothing durable attached, and regenerating means asking for a fresh take. Recorded it in a comment so it reads as a decision rather than an accident of how the update was written.
- Built approve and edit-then-approve as one endpoint on the existing version-checked transaction rather than a second write path: the same apply writes the country override and clears the pending suggestion in one atomic step, and an edit just swaps in the manager's value before the same type-check. Made it require a still-pending suggestion instead of trusting whatever value the request carried, so approving a country with nothing pending returns a 404 rather than silently writing an arbitrary override. Proved both the 404 guard and the atomic clear with curl.
- Decided a reject only deletes panel-only suggestion metadata, so it neither bumps version nor goes through the version check: it touches nothing a mobile client or a concurrent value-edit depends on, and bumping would spuriously 409 an unrelated edit. Kept it symmetric with generation, which also writes suggestions without touching version. Came down against reusing the frontend's 409 UX for reject even though the symmetry was tempting, and confirmed by curl that a reject left version untouched.
- Found the fence fix I'd logged earlier hadn't actually taken effect. I was slicing the first-brace-to-last-brace substring but still calling JSON.parse on the raw text, so it 502'd again the moment the model wrapped its output in a ```json fence. A one-word miss (parsed text, not the extracted json). Confirmed by calling Anthropic directly and seeing a clean 200 with the fence, then re-ran generation after the fix.
- Testing the new suggestion UI, a two-tab conflict run made Generate look dead. Clicking it did nothing. The immediate trigger was just an empty country field, but the reason I couldn't see that was the suggestion section had no error display at all, so every failure path was silent. Added the missing message alongside the override half's existing one, and the empty-input guard then said exactly what was wrong. Approve and reject failures had been just as invisible until I added it.
- My approve handler turned every 404 into a "parameter deleted" dialog, but the backend returns 404 for two different things, the parameter being gone and the suggestion already handled in another tab. Rather than match on the error text, which a later reword could quietly break, I reload and check whether the parameter still exists: absent means deleted, present means the row was just resolved elsewhere. Scoped it to approve only, since that's the one endpoint with two 404 meanings.
- Chose table-to-cards for the mobile layout over a horizontal-scrolling table, matching the provided mobile screenshot, and did it CSS-only with data-label attributes so the same table markup restyles into cards rather than duplicating each row's edit/override/delete controls into a separate mobile block.
- Had Claude re-read the README against the actual backend before submission. It caught that the .env block documented four variables while the code reads seven, and the missing API_TOKEN is the one the server refuses to start without, so a fresh clone following the README wouldn't boot. It also flagged the schema sample naming gpt-4o-mini for the AI suggestions when they run on Anthropic, so I corrected it to the Claude model the response actually records.
- Nearly ticked off the two-tab delete check using the wrong test. I'd edited the same parameter in both tabs and got the version-conflict dialog, but that exercises the 409 path, not the edit-versus-delete interleaving the README claims is safe. Re-ran it as edit-in-one-tab, delete-in-the-other, then save, and confirmed the save returns a recoverable 404 with the row staying deleted rather than resurrected.
- After redeploying, verified the four scenarios against the live /config rather than trusting the panel: a TR override resolves while an unknown country falls back to the default, number and boolean values come back correctly typed with the panel-only fields stripped, and a deleted parameter is gone from the served config. Nothing was broken this time, but I ran it because the live build has been the thing actually broken on previous days.

## 22/07/2026

- Fixed a create that 500'd when the description was omitted, since Firestore rejects an undefined field. The obvious fix, which the error message itself and Claude both pointed at, was initializeFirestore(app, { ignoreUndefinedProperties: true }). It did nothing and the 500 persisted. Isolated it in a throwaway script and found that settings argument is a silent no-op in firebase-admin 14; db.settings({ ignoreUndefinedProperties: true }) is what actually takes. Chose ignoring undefined app-wide over defaulting description to an empty string, so every optional field is covered rather than just this one, and confirmed the create returns 201 with the field simply dropped.
- Replaced the serving API token's plain !== with a timing-safe compare so it no longer short-circuits on the first differing character. Went with hashing both sides to a fixed length and comparing those, rather than timingSafeEqual on the raw tokens behind a length guard, since the guard both leaks the token's length and throws on unequal-length inputs. Removed the item from the README's limitations now that it's closed rather than fixed-but-still-listed.
- Noticed I could create a parameter with no value even though the form says value is required. Traced it to the shared value input: the number and json cases reject an empty entry but the string case never marked itself invalid, so an empty string passed. Fixed it in the component rather than the create handler so the override editor and the edit-before-approve step inherit the same rule instead of me repeating the check.
- The Edit/Override/Delete buttons wrapped raggedly at tablet widths. Rather than force them onto one line and risk a horizontal scrollbar on narrower desktops, raised the table's card breakpoint to 1024px so those in-between widths get the clean card layout instead of a squeezed table.
